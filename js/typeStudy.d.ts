declare const jQuery: (selector: string) => any
declare const jName: (age: number) => number
declare class Animal {
  name: string
}
declare enum Directios {
  Up,
  Down,
  Left,
  Right,
}
declare interface aaName {
  name: string
}

declare enum Directions {
  Up,
  Down,
  Left,
  Right,
}
